# 项目协作与发布规范

本文件适用于整个 MenuCal 仓库。执行开发、验证、提交和发布操作时，必须遵守以下规则。

## GitHub 页面 README 修改权限

- 此处 README 特指 GitHub 仓库页面向用户展示的 README（通常对应仓库根目录的 README.md），不泛指项目中所有内部说明文件。
- 未经用户明确允许，不得修改、重写、删除或自动更新该 README；无论通过本地文件提交推送还是直接在线编辑，都适用此限制。
- 开发功能、修复问题、调整版本号、打包或发布新版本，都不构成修改 README 的授权。
- 如果确实需要更新 README，先说明需要修改的内容并获得用户许可。

## 新版本发布

- 用户决定发布新版本时，交付必须同时包含源代码推送和 GitHub Releases 发布页面上的 DMG 安装包。
- 仅推送分支、提交 PR、在本地生成 DMG，或上传 CI 构建产物，都不算完成发布。
- 为对应版本创建或更新 GitHub Release，将该版本的 DMG 上传为 Release 附件，方便用户直接下载。
- DMG 应包含 MenuCal.app 和指向 Applications 的安装快捷方式；版本信息应与发布版本一致。
- 发布前可通过命令行检查构建结果、签名、DMG 完整性和安装包内容，不得因此主动进行图形化验证。
- 完成后提供 GitHub Release 页面链接及 DMG 下载链接，并如实说明签名、公证状态和必要的首次打开步骤。

## 发布提交中的测试代码

- 用户决定发布的新版本，不向 GitHub 提交或推送新增、修改的测试相关代码，包括测试文件、测试辅助代码和仅用于测试的配置。
- 暂存和提交前检查文件清单与差异，将测试相关改动排除在发布提交之外；避免使用不加区分的全量暂存。
- 可以在本地运行已有测试作为非图形化验证，但测试结果不改变上述提交范围。
- 不得将此规则解释为删除仓库中已有的测试、清除本地测试改动或改写历史；这些操作需要用户另行要求。
- 若待推送的已有提交包含测试相关改动，先整理符合上述范围的发布提交，再推送，保留本地原始工作。

## Computer Use 与 UI 验证

- 除非用户主动、明确要求，不得为任何代码的增、删、改、查使用 Computer Use 进行图形化操作或验证。
- 该限制包括通过桌面应用、浏览器或其他 GUI 自动化方式查看界面、点击控件、截图检查和验证交互；不得换用其他 GUI 工具绕过。
- “开发”“修复”“检查”“测试”“打包”“发布”等一般性请求，不构成 Computer Use 或图形化验证的授权。
- UI 由用户自行验证，并反馈具体问题；根据用户反馈修改代码。
- 可使用源码检查、命令行构建、非图形化测试、日志及包结构检查完成必要验证，并明确区分这些结果与用户尚未验证的 UI 表现。
